---
title: useCallback
slug: /hooks/usecallback
---

# useCallback

Returns a memoized callback function

## Hook Information

**Category:** performance

**Type:** React Built-in Hook


## Hook Usage in Codebase

Found **3 usage(s)** across your codebase:

### Usage 1

**File:** `src\utils\useFormValidator.ts` (line 20)

**Context:** Used in `validateField` function

```typescript
const validateField = useCallback((fieldName: keyof T, value: T[typeof fieldName]) => {
```

**Parameters used:** `() => {...}`, `[validationRules]`

**Default values:**

- `callback`: `function`
- `initialArray`: `[validationRules]`

### Usage 2

**File:** `src\utils\useFormValidator.ts` (line 32)

**Context:** Used in `validateAll` function

```typescript
const validateAll = useCallback((values: T) => {
```

**Parameters used:** `() => {...}`, `[validationRules]`

**Default values:**

- `callback`: `function`
- `initialArray`: `[validationRules]`

### Usage 3

**File:** `src\utils\useFormValidator.ts` (line 49)

**Context:** Used in `clearErrors` function

```typescript
const clearErrors = useCallback(() => {
```

**Parameters used:** `() => {...}`, `[]`

**Default values:**

- `callback`: `function`
- `initialArray`: `[]`



## Source
`React Built-in`