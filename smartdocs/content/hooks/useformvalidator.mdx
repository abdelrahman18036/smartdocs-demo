---
title: useFormValidator
slug: /hooks/useformvalidator
---

# useFormValidator

Custom hook for managing form validation state

## Hook Information

**Category:** custom

**Type:** Custom Hook


## Parameters

| Name | Type | Required | Description |
|------|------|----------|-------------|
| `validationRules` | `unknown` | Yes |  |


## Return Type

**Type:** `any`

## Hook Usage in Codebase

Found **3 usage(s)** across your codebase:

### Usage 1

**File:** `src\component\InputField.tsx` (line 46)

**Context:** Used in `InputField` function

```typescript
const { validateField, errors } = useFormValidator({
```

**Destructured variables:** `validateField`, `errors`

**Parameters used:** `{input: ...}`

**Default values:**

- `input`: `...`

### Usage 2

**File:** `src\pages\ContactPage.tsx` (line 28)

**Context:** Used in `ContactPage` function

```typescript
const { validateField, errors, isValid, validateAll } = useFormValidator({
```

**Destructured variables:** `validateField`, `errors`, `isValid`, `validateAll`

**Parameters used:** `{name: ..., email: ..., company: ..., subject: ..., message: ...}`

**Default values:**

- `name`: `...`
- `email`: `...`
- `company`: `...`
- `subject`: `...`
- `message`: `...`

### Usage 3

**File:** `src\pages\ExamplePage.tsx` (line 29)

**Context:** Used in `ExamplePage` function

```typescript
const { validateField, isValid } = useFormValidator({
```

**Destructured variables:** `validateField`, `isValid`

**Parameters used:** `{email: ..., name: ...}`

**Default values:**

- `email`: `...`
- `name`: `...`



## Real Usage Examples

### Usage 1

```typescript
const { validateField, errors } = useFormValidator({
    input: (val: string) => {
      if (required && !val.trim()) return 'This field is required';
      if (type === 'email' && val && !/\S+@\S+\.\S+/.test(val)) return 'Invalid email format';
      return null;
    }
  });
```

### Usage 2

```typescript
const { validateField, errors, isValid, validateAll } = useFormValidator({
    name: (value: string) => {
      if (!value.trim()) return 'Name is required'
      if (value.length < 2) return 'Name must be at least 2 characters'
      return null
    },
    email: (value: string) => {
      if (!value.trim()) return 'Email is required'
      if (!/\S+@\S+\.\S+/.test(value)) return 'Please enter a valid email address'
      return null
    },
    company: (value: string) => {
      if (value && value.length < 2) return 'Company name must be at least 2 characters'
      return null
    },
    subject: (value: string) => {
      if (!value.trim()) return 'Subject is required'
      if (value.length < 5) return 'Subject must be at least 5 characters'
      return null
    },
    message: (value: string) => {
      if (!value.trim()) return 'Message is required'
      if (value.length < 10) return 'Message must be at least 10 characters'
      if (value.length > 1000) return 'Message must be less than 1000 characters'
      return null
    }
  })
```

### Usage 3

```typescript
const { validateField, isValid } = useFormValidator({
    email: (value: string) => /\S+@\S+\.\S+/.test(value) ? null : 'Invalid email',
    name: (value: string) => value.length >= 2 ? null : 'Name too short'
  })
```

## Source
`src\utils\useFormValidator.ts`