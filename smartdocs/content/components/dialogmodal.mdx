---
title: DialogModal
slug: /components/dialogmodal
---

# DialogModal

Dialog modal component for displaying overlay content

## Real Usage Examples

### Usage 1

```tsx
<DialogModal
isOpen={showModal}
onClose={() => setShowModal(false)}
title="Demo Modal"
maxWidth="md"
>
<div className="space-y-4">
<p>This is a dialog modal component that was opened by clicking the button.</p>
<p>It demonstrates:</p>
<ul className="list-disc list-inside space-y-1 text-sm">
<li>Backdrop click to close</li>
<li>Custom title and content</li>
<li>Responsive sizing</li>
<li>Proper accessibility</li>
<li>Integration with global context</li>
</ul>
<div className="flex justify-end space-x-2">
<ButtonComponent
variant="secondary"
onClick={() => {
setShowModal(false)
addNotification({ message: 'Modal closed', type: 'info' })
}}
>
Cancel
</ButtonComponent>
<ButtonComponent
variant="primary"
onClick={() => {
setShowModal(false)
addNotification({ message: 'Modal confirmed!', type: 'success' })
}}
>
OK
</ButtonComponent>
</div>
</div>
</DialogModal>
```

### Usage 2

```tsx
<DialogModal
isOpen={showWelcomeModal}
onClose={() => setShowWelcomeModal(false)}
title="Welcome to SmartDocs Demo!"
maxWidth="md"
>
<div className="space-y-4">
<p>Thank you for exploring our demo application!</p>
<p>This app demonstrates:</p>
<ul className="list-disc list-inside space-y-1 text-sm">
<li>React Router for navigation</li>
<li>Context API for global state</li>
<li>TypeScript integration</li>
<li>Component composition</li>
<li>Responsive design with Tailwind CSS</li>
</ul>
<div className="flex justify-end space-x-2">
<ButtonComponent variant="primary" onClick={() => setShowWelcomeModal(false)}>
Got it!
</ButtonComponent>
</div>
</div>
</DialogModal>
```

## Props

| Name | Type | Required | Default | Description |
|------|------|----------|---------|-------------|
| `isOpen` | `boolean` | Yes | - | Whether the modal is currently visible |
| `onClose` | `() => void` | Yes | - | Function called when the modal should be closed |
| `title` | `string` | No | - | Optional title for the modal header |
| `children` | `ReactNode` | Yes | - | The content to display in the modal body |
| `maxWidth` | `enum` | No | `md` | Maximum width of the modal |
| `closeOnBackdropClick` | `boolean` | No | `true` | Whether clicking the backdrop should close the modal |


## Source
`src\component\DialogModal.tsx`